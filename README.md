# Code Review Tool

A web application that visualizes the differences between commits in a GitHub repository. Each file appears as a row, and each commit appears as a column, making it easy to see which files were changed in which commits at a glance.
